import RentalLayout from './components/RentalLayout'; // If you put the previous code in a separate file
// OR just use the layout directly if you want everything in App.jsx

function App() {
  return (
    <RentalLayout />
  );
}

export default App;
